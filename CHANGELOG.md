# Changelog

## 2026-08-25

### เพิ่ม

- **ข้อมูลอุปกรณ์เพิ่ม 3 ช่อง** — หมายเลขไอดี (ตั้งเองได้ให้ตรงกับทะเบียนเดิม เว้นว่าง = ระบบแจกให้),
  หมายเลขวงจร และจุดพิกัด
- **ปักหมุดพิกัดบนแผนที่** — Leaflet + OpenStreetMap ค้นหาสถานที่ด้วย Nominatim ใช้ฟรีไม่ต้องมี API key
  คลิกปักหมุด ลากปรับตำแหน่ง หรือใช้ตำแหน่งปัจจุบันก็ได้ พิกัดในตารางกดเปิด Google Maps ได้เลย
- **ปุ่มแก้ไขอุปกรณ์** — เดิมต้องลบแล้วเพิ่มใหม่ ซึ่งทำให้เลข ID เปลี่ยนแล้วต้องตามไปแก้ MikroTik
  ตอนนี้แก้ได้ทุกช่องรวมถึงเปลี่ยนเลข ID โดยสถานะและเวลารายงานล่าสุดไม่ถูกแตะ
- **กดดูรายละเอียดจากหน้าแผงควบคุม** — กดตัวเลข UP/DOWN/UNKNOWN บนการ์ดแล้วเห็นรายชื่ออุปกรณ์
  ในกลุ่มนั้น พร้อมวงจร IP เวลารายงานล่าสุด และลิงก์เปิดพิกัด
- **กระดิ่งแจ้งเตือนกางดูรายตัวได้** ว่าอุปกรณ์ไหน down อยู่ที่ไหน และจำว่าหมวดไหนกางไว้
- **ตรวจ "ขาดการติดต่อ"** — อุปกรณ์ที่ไม่มีรายงานเข้ามาเกินเวลาที่ตั้งไว้จะถูกนับเป็น UNKNOWN
  แทน UP/DOWN เพราะไม่รู้สถานะจริงแล้ว (MikroTik ดับ / เน็ตขาด / สคริปต์หาย)
  ตั้งเวลาได้ที่เมนูการตั้งค่า ค่าเริ่มต้น 3 นาที ใส่ 0 เพื่อปิด
  **ต้องลงสคริปต์ Scheduler ครบทุกตัวก่อนใช้**
- **เมนูการตั้งค่ามีทั้งสองหน้าแล้ว** เดิมมีแค่หน้าแผงควบคุม พอสลับไปหน้าอุปกรณ์เมนูจะหายไป
- กด **Esc** ปิดหน้าต่างที่เปิดอยู่ได้

### แก้

- **เวลารายงานล่าสุดเพี้ยน 7 ชั่วโมง** — โฮสต์รันเป็น UTC แต่เก็บเวลาแบบไม่มี timezone
  หน้าเว็บเลยเอาไปแสดงดิบๆ (Netwatch ขึ้น 10 โมง เว็บขึ้นตี 3)
  เปลี่ยนมาเก็บเป็น UTC พร้อม offset แล้วให้เบราว์เซอร์แปลงเป็นเวลาเครื่องที่เปิดดู
- **ช่องกรอกล้นกรอบบนมือถือ** — `grid-template-columns: 1fr` มีค่าต่ำสุดเท่ากับ min-content
  ของช่องที่กว้างที่สุด คอลัมน์เลยกว้าง 304px ทั้งที่กรอบมี 223px แก้เป็น `minmax(0, 1fr)`

### เปลี่ยน

- **หน้าต่างยืนยันก่อนลบอุปกรณ์** แทน `confirm()` ของเบราว์เซอร์ บอกชื่อ/หมวด/วงจร/IP
  ของตัวที่จะลบ และเตือนให้ไปลบ Netwatch ฝั่ง MikroTik ด้วย
- ตัวเลือก "ดึงข้อมูลอัตโนมัติ" มีผลกับหน้าอุปกรณ์ด้วยแล้ว เดิมตารางยิงซ้ำทุก 15 วินาทีตายตัว
- แถบสถานะบนหัวหน้าแผงควบคุมไม่ใช่ปุ่มแล้ว (เดิมกดแล้วเด้งไปหน้าตั้งค่าซึ่งไม่เกี่ยวกัน)

### เอาออก

- **โหมดดึงข้อมูลจาก API ภายนอกทั้งชุด** — ช่องตั้งค่า URL/Key, ปุ่มทดสอบเชื่อมต่อ,
  `/api/config/test`, ตัว proxy, ข้อมูลตัวอย่าง และตัว normalize หลายรูปแบบ
  สถานะมาจาก MikroTik ทางเดียวอยู่แล้ว และ `api_url`/`api_key` ที่ค้างใน config ถูกล้างทิ้ง
  เพราะ key เป็นความลับไม่ควรค้างไว้
