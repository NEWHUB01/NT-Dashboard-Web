# Changelog

## 2026-08-25 (รอบที่ 4 — ความปลอดภัย)

- **อุดช่องโหว่: ดึงไฟล์ความลับได้โดยไม่ต้องล็อกอิน** — route ไฟล์หน้าเว็บเสิร์ฟไฟล์อะไรก็ได้
  ในโฟลเดอร์โปรเจกต์ ทำให้ `GET /config.json` คืน secret key ที่ใช้เซ็น session
  และ `GET /users.json` คืน hash รหัสผ่าน ให้ใครก็ได้ที่รู้ URL
  ใครได้ secret key ไปก็ปลอม cookie เข้าเป็น admin ได้ทันที
  แก้เป็น allowlist เสิร์ฟเฉพาะ 9 ไฟล์ที่เป็นหน้าเว็บจริงๆ
  (กระทบเฉพาะโหมดรันเองที่มีไฟล์อยู่บนดิสก์ — บน Vercel ความลับอยู่ใน env var ไฟล์พวกนี้จึงไม่มี)
- บังคับ session cookie ให้ส่งเฉพาะทาง https เมื่อ deploy บนโฮสต์จริง
  (โหมดรันเองในวง LAN เป็น http จึงไม่บังคับ ไม่งั้นล็อกอินไม่ได้)

## 2026-08-25 (รอบที่ 3 — ฝั่ง MikroTik)

- **รับรายงานรวมหลายอุปกรณ์ในคำขอเดียว** — `/status/mikrotik.php` รับ `batch=1:up,2:down,...`
  ได้แล้ว (ทั้งทาง query string และ body ของ POST) อ่านทะเบียนครั้งเดียว เขียนกลับครั้งเดียว
  ไม่ว่าจะมีกี่อุปกรณ์ ของเดิมที่ยิงทีละตัวยังใช้ได้เหมือนเดิมไม่ต้องแก้อะไร
- **ปุ่ม "โค้ด Scheduler รวมทุกตัว"** ที่หัวตารางอุปกรณ์ สร้างสคริปต์ RouterOS ตัวเดียว
  ที่วนอ่าน Netwatch ทุกรายการแล้วส่งรวดเดียว พร้อมเตือนให้ลบ Scheduler เดิม
  ที่แยกรายตัวออกก่อน และบอกว่ามีอุปกรณ์กี่ตัวที่ยังไม่ได้กรอก IP ในทะเบียน
- **วางสคริปต์ครั้งเดียวจบ** — สคริปต์ไม่ได้ฝังรายชื่ออุปกรณ์ไว้ แต่ส่ง IP ที่อ่านได้จาก
  Netwatch มาให้เซิร์ฟเวอร์จับคู่กับทะเบียนเอง เพิ่มอุปกรณ์ทีหลังจึงไม่ต้องมา copy วางใหม่
  (endpoint รับได้ทั้งแบบ IP และแบบหมายเลขไอดี รวมถึง host ที่เป็น IPv6)
- ที่ 4 อุปกรณ์: คำสั่งฐานข้อมูลฝั่ง MikroTik ลดจาก 345,600 เหลือ 86,400 ต่อเดือน
  และจะไม่โตอีกเลยเมื่อเพิ่มอุปกรณ์

## 2026-08-25 (รอบที่ 2 — performance)

- **หน้าแผงควบคุมยิงครั้งเดียวต่อรอบ** เดิมยิง `/api/status` กับ `/api/devices` ควบกัน
  ทั้งที่ยอดรวมนับจากรายตัวได้อยู่แล้ว — ตัด `/api/status` ทิ้ง อ่านที่เก็บข้อมูลลดจาก 4 เหลือ 1 ครั้งต่อรอบ
  ผลพลอยได้: เลขบนการ์ดกับรายการที่กดเข้าไปดูมาจากข้อมูลชุดเดียวกันเสมอ ไม่เหลื่อมกันอีก
- **หยุดดึงข้อมูลเมื่อแท็บถูกซ่อน** เดิมเปิดทิ้งไว้ทั้งวันก็ยิงทุก 15 วินาทีตลอด
  ตอนนี้ไม่มีคนดู = ไม่ยิงเลย และดึงให้ทันทีตอนกลับมาที่แท็บ
- **แคชค่าเพดานขาดการติดต่อไว้ 60 วินาที** เดิมอ่าน config จากที่เก็บข้อมูลทุก request
  แค่เพื่อเอาเลขตัวเดียว (แก้ค่าในหน้าตั้งค่าแล้วมีผลกับ instance นั้นทันที)
- **ตารางอุปกรณ์แก้เฉพาะช่องที่ค่าเปลี่ยน** เดิมล้างแล้วสร้างใหม่ทั้งหมดทุก 15 วินาที
  (900 อุปกรณ์ = 8,100 ช่อง + 2,700 ปุ่มพร้อม listener ใช้เวลา ~350 ms)
  ตอนนี้ไม่มีอะไรเปลี่ยน = แตะ DOM 0 ครั้ง, เปลี่ยน 1 ตัว = 7 ครั้ง
  และปุ่มในตารางใช้ listener ตัวเดียวที่ตัวตารางแทนผูกทีละปุ่ม
- **โหลดแผนที่ตอนกดปักหมุดครั้งแรก** เดิมโหลด Leaflet 36 KB ติดมาทุกครั้งที่เปิดหน้าอุปกรณ์
  ทั้งที่ส่วนใหญ่เข้ามาดูตารางเฉยๆ

## 2026-08-25

### เพิ่ม

- **ข้อมูลอุปกรณ์เพิ่ม 3 ช่อง** — หมายเลขไอดี (ตั้งเองได้ให้ตรงกับทะเบียนเดิม เว้นว่าง = ระบบแจกให้),
  หมายเลขวงจร และจุดพิกัด
- **ปักหมุดพิกัดบนแผนที่** — Leaflet + OpenStreetMap ค้นหาสถานที่ด้วย Nominatim ใช้ฟรีไม่ต้องมี API key
  คลิกปักหมุด ลากปรับตำแหน่ง หรือใช้ตำแหน่งปัจจุบันก็ได้ พิกัดในตารางกดเปิด Google Maps ได้เลย
- **ปุ่มแก้ไขอุปกรณ์** — เดิมต้องลบแล้วเพิ่มใหม่ ซึ่งทำให้เลข ID เปลี่ยนแล้วต้องตามไปแก้ MikroTik
  ตอนนี้แก้ได้ทุกช่องรวมถึงเปลี่ยนเลข ID โดยสถานะและเวลารายงานล่าสุดไม่ถูกแตะ
- **กดดูรายละเอียดจากหน้าแผงควบคุม** — กดตัวเลข UP/DOWN/UNKNOWN บนการ์ดแล้วเห็นรายชื่ออุปกรณ์
  ในกลุ่มนั้น พร้อมวงจร IP เวลารายงานล่าสุด และลิงก์เปิดพิกัด
- **กระดิ่งแจ้งเตือนกางดูรายตัวได้** ว่าอุปกรณ์ไหน down อยู่ที่ไหน และจำว่าหมวดไหนกางไว้
- **ตรวจ "ขาดการติดต่อ"** — อุปกรณ์ที่ไม่มีรายงานเข้ามาเกินเวลาที่ตั้งไว้จะถูกนับเป็น UNKNOWN
  แทน UP/DOWN เพราะไม่รู้สถานะจริงแล้ว (MikroTik ดับ / เน็ตขาด / สคริปต์หาย)
  ตั้งเวลาได้ที่เมนูการตั้งค่า ค่าเริ่มต้น 3 นาที ใส่ 0 เพื่อปิด
  **ต้องลงสคริปต์ Scheduler ครบทุกตัวก่อนใช้**
- **เมนูการตั้งค่ามีทั้งสองหน้าแล้ว** เดิมมีแค่หน้าแผงควบคุม พอสลับไปหน้าอุปกรณ์เมนูจะหายไป
- กด **Esc** ปิดหน้าต่างที่เปิดอยู่ได้

### แก้

- **เวลารายงานล่าสุดเพี้ยน 7 ชั่วโมง** — โฮสต์รันเป็น UTC แต่เก็บเวลาแบบไม่มี timezone
  หน้าเว็บเลยเอาไปแสดงดิบๆ (Netwatch ขึ้น 10 โมง เว็บขึ้นตี 3)
  เปลี่ยนมาเก็บเป็น UTC พร้อม offset แล้วให้เบราว์เซอร์แปลงเป็นเวลาเครื่องที่เปิดดู
- **ช่องกรอกล้นกรอบบนมือถือ** — `grid-template-columns: 1fr` มีค่าต่ำสุดเท่ากับ min-content
  ของช่องที่กว้างที่สุด คอลัมน์เลยกว้าง 304px ทั้งที่กรอบมี 223px แก้เป็น `minmax(0, 1fr)`

### เปลี่ยน

- **หน้าต่างยืนยันก่อนลบอุปกรณ์** แทน `confirm()` ของเบราว์เซอร์ บอกชื่อ/หมวด/วงจร/IP
  ของตัวที่จะลบ และเตือนให้ไปลบ Netwatch ฝั่ง MikroTik ด้วย
- ตัวเลือก "ดึงข้อมูลอัตโนมัติ" มีผลกับหน้าอุปกรณ์ด้วยแล้ว เดิมตารางยิงซ้ำทุก 15 วินาทีตายตัว
- แถบสถานะบนหัวหน้าแผงควบคุมไม่ใช่ปุ่มแล้ว (เดิมกดแล้วเด้งไปหน้าตั้งค่าซึ่งไม่เกี่ยวกัน)

### เอาออก

- **โหมดดึงข้อมูลจาก API ภายนอกทั้งชุด** — ช่องตั้งค่า URL/Key, ปุ่มทดสอบเชื่อมต่อ,
  `/api/config/test`, ตัว proxy, ข้อมูลตัวอย่าง และตัว normalize หลายรูปแบบ
  สถานะมาจาก MikroTik ทางเดียวอยู่แล้ว และ `api_url`/`api_key` ที่ค้างใน config ถูกล้างทิ้ง
  เพราะ key เป็นความลับไม่ควรค้างไว้
